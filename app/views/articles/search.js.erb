$('#articles').html("<%= escape_javascript(render(:partial => 'search_result')) %>");

// $('div.load.hide').removeClass('hide')
// // Timeout is just for show since search is fast locally
// setTimeout(
//   function()
//   {
//     $('div.load').addClass('hide')
//   }, 1000);
